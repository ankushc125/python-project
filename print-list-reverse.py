# Initialize a list with a set of numbers
numbers = [1, 2, 3, 4, 5]

# Reverse the list
reversed_numbers = numbers[::-1]

# Print the original and reversed lists
print("Original list:", numbers)
print("Reversed list:", reversed_numbers)
